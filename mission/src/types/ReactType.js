import { node, element, elementType } from 'prop-types';

// React 컴포넌트가 렌더링 가능한 모든 타입
export const ReactNodeType = node;

// React 요소 타입
export const ReactElementType = element;

// React 컴포넌트 타입
export const ReactComponentType = elementType;
